/**
 * ════════════════════════════════════════════════════════════════
 *  站点配置 —— 这是你唯一需要改的文件
 * ════════════════════════════════════════════════════════════════
 *
 * 改完存盘就生效（本地跑着 npm run dev 的话会立刻刷新）。
 *
 * 不想手动改文件？在 site 目录下运行：
 *
 *     npm run section
 *
 * 会给你一个菜单，可以增、删、改名、换色、调顺序，它会把本文件重写好。
 *
 * ── 每个栏目要填什么 ────────────────────────────────────────────
 *
 *   id      必填。网址的一部分，只能是英文小写字母、数字和连字符。
 *           id 是 'photo'，栏目页地址就是 /photo/
 *           ⚠️ 已经有内容的栏目，别改 id —— 内容是按 id 归类的，
 *              改了它们就找不到家了（改名前先问你：要不要我帮你把
 *              已有内容一起迁移）。
 *
 *   label   必填。显示给人看的中文名。
 *
 *   desc    一句话说明，出现在首页的入口卡上。
 *
 *   glyph   一个汉字，做成入口卡的图标。
 *
 *   color   这个栏目的主色，十六进制。每个栏目一个颜色，
 *           进到栏目页会和别的栏目长得不一样。
 *
 *   layout  列表怎么排，三选一：
 *             'stream'    一行一条，适合文字、笔记
 *             'gallery'   卡片网格，适合照片、作品
 *             'timeline'  按月份分组，适合日记、流水
 *
 *   order   先后顺序，数字小的排前面。
 *
 * ── 加一个栏目怎么做 ────────────────────────────────────────────
 *
 *   在下面的数组里加一行即可，例如加一个「收藏」：
 *
 *     { id: 'collection', label: '收藏', desc: '收着的东西',
 *       glyph: '藏', color: '#7a6f9a', layout: 'gallery', order: 50 },
 *
 *   导航栏、首页入口、栏目页、标签统计都会自动出现，不用改别的地方。
 *
 * ── 删一个栏目 ──────────────────────────────────────────────────
 *
 *   直接删掉那一行。该栏目下已有的文章文件会留在磁盘上，
 *   但因为 id 没了，它们不会出现在网站上（没有丢失，加回来就又能看到）。
 */

export const SECTIONS = [
  {
    id: "writing",
    label: "文字",
    desc: "随笔、长文、笔记",
    glyph: "文",
    color: "#9a6b3f",
    layout: "stream",
    order: 10,
  },
  {
    id: "photo",
    label: "照片",
    desc: "按时间与事件归类",
    glyph: "影",
    color: "#5b7c99",
    layout: "gallery",
    order: 20,
  },
  {
    id: "work",
    label: "创作",
    desc: "作品、项目、过程记录",
    glyph: "作",
    color: "#8a5a6b",
    layout: "gallery",
    order: 30,
  },
  {
    id: "life",
    label: "生活",
    desc: "流水、里程碑、清单",
    glyph: "生",
    color: "#5f8256",
    layout: "timeline",
    order: 40,
  },
]

/**
 * 默认栏目：写内容时不填 kind，就归到这里。
 * 必须是上面某个栏目的 id。
 */
export const DEFAULT_SECTION = "writing"
